### 1. User Story
**Como** Usuario Nuevo
**Quiero** registrar mis datos personales en la aplicación
**Para** comenzar a operar como cliente particular o corporativo.

### 2. Descripción de la Funcionalidad (Flow)
#### Flujo de Registro (Onboarding):
1. El usuario ingresa a la pantalla de Registro y escribe su Email.
2. Al perder el foco del input de email (o hacer siguiente), el frontend consulta si el email está pre-asignado a una empresa (configurado en **US-02**).
3. **Camino A (Email Corporativo)**:
   - Si está asignado, los campos "Empresa" y "Tipo de Menú" se muestran pre-cargados y bloqueados.
   - El backend creará automáticamente la primera dirección apuntando a la empresa.
4. **Camino B (Email Particular)**:
   - Si no está asignado, esos campos se ocultan o quedan libres como Usuario Particular.
5. El usuario completa los datos obligatorios: Nombre, Apellido, Teléfono, DNI, y contraseña. Además de ingresar su primera Dirección de Entrega (solo si es particular, o si es corporativo con permisos de direcciones extra).
6. Envía el formulario. El backend registra al usuario y devuelve el token de sesión.

### 3. Lista Completa de Campos
*   `email`: String (email).
*   `password`: String (min 8 chars).
*   `firstName`: String.
*   `lastName`: String.
*   `phone`: String.
*   `dni`: String.
*   `address`: Objeto (street, number, locality, reference).

### 4. Estructura de Endpoints y URLs

#### 4.1 Verificar pre-asignación: `POST /api/auth/check-email`
- **Request Body**:
```json
{ "email": "juan@acme.com" }
```
- **Success Response (200 OK)**:
```json
{
  "success": true,
  "data": {
    "isCorporate": true,
    "companyName": "ACME S.A.",
    "benefitType": "Corporativo Premium"
  }
}
```

#### 4.2 Registro: `POST /api/auth/register`
- **Request Body**:
```json
{
  "email": "juan@acme.com",
  "password": "securepassword123",
  "firstName": "Juan",
  "lastName": "Pérez",
  "phone": "3815555555",
  "dni": "30123456",
  "address": {
    "street": "San Martín", "number": "123", "locality": "Lules", "reference": "Oficina central"
  }
}
```

### 5. Archivos/Módulos Modificados (Arquitectura DDD)

| Capa | Archivos |
|------|----------|
| **Dominio** | `User.ts`, `Address.ts` |
| **Aplicación** | `authService.ts`, `addressService.ts` |
| **Infraestructura** | `SequelizeUserRepository.ts` |
| **Presentación** | `authController.ts`, `authRoutes.ts` |
| **Frontend** | `Register.tsx`, `Register.css` |

### 6. Definition of Done (DoD)
*   [ ] Validación en tiempo real del correo electrónico contra BD corporativa.
*   [ ] Generación automática de dirección si es perfil corporativo.
*   [ ] Bloqueo de campos corporativos en el frontend.
*   [ ] Creación de usuario y autenticación inmediata tras el registro.
